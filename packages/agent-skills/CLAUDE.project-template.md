# CLAUDE.md — <PROJECT NAME>

> Layer 3 (project facts) of the agent context model. This file is a **local
> dev-orientation render**, generated/refreshed from the RoadBoard project
> summary during onboarding. RoadBoard (via its MCP tools) remains the single
> source of truth for project management (tasks, phases, memory, decisions,
> architecture). Do **not** hand-maintain project-management data here.

## What this project is
<one-paragraph summary: domain, what it does, current maturity>

## Stack
<languages, frameworks, datastores, infra — concrete versions where they matter>

## Layout
<top-level dirs / packages / services and what each is for — the map, not every file>

## Run / build / test
```sh
# install
<cmd>
# dev
<cmd>
# build
<cmd>
# test / lint
<cmd>
```

## Entry points
<where execution starts: main(s), servers, CLIs, key modules to read first>

## Conventions
<commit format, branch model, code style, naming, anything an agent must respect — keep in sync with the git-conventions skill>

## Gotchas
<non-obvious traps: env vars, ports, build watchdogs, quoting issues, things that broke before>

## RoadBoard
- Project id: `<id>`
- Refresh this file from RoadBoard: `prepare_project_summary` + `get_project_memory` via the MCP tools.
